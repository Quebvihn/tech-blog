{{#each blogs as |blog|}}

  <div class="row p-2">

    <div class="container blogBox">
			<div class="row  justify-content-center headerRow p-3">
      <div class="col-8">
        <h2>
          <a class="blogTitle text-uppercase" href="/blog/{{blog.id}}">{{blog.title}}</a>
        </h2>
      </div>

      <div class="col-4">
        <p class="blogCreate text-right">Created by
          {{blog.user.username}}
          on
          {{format_date blog.date_created}}
        </p>
      </div>
			</div>
    <div class="blogDesc p-2">
      <p class="text-center blogDescMain">
        {{blog.description}}
      </p>
    </div>
    </div>

  </div>

{{/each}}